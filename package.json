{
  "name": "cs2-stata",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "collect": "node scripts/collect.mjs",
    "add": "node scripts/collect.mjs --add"
  }
}
